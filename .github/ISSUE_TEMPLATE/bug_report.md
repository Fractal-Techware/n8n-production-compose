---
name: Stack bug
about: A service does not start, HTTPS does not come up, or a setting is wrong
labels: bug
---

**What happened / what you expected:**

**Versions** (`docker version`, `docker compose version`, host OS):

**Your .env with every secret removed:**

```
```

**Output** (`./scripts/check-config.sh`, `docker compose ps`, relevant `docker compose logs`):

<!-- Never paste your encryption key, database password or any workflow credential. -->
